## 1.0.2

- Marked package as **discontinued**.
- Added `replaced_by: flutter_flavor_manager` in `pubspec.yaml`.
- Updated README to warn users that this package is no longer maintained.
- All public APIs now throw `UnsupportedError` to prevent further usage.
